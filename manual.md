# 教務パートナー v18 操作マニュアル

日本の小・中・高校の先生のための教務支援アプリ「教務パートナー」の詳しい操作説明です。
このアプリは **index.html 1枚** で動作し、データはすべて**お使いのブラウザ内**に保存されます。

---

## 1. はじめに

### 1.1 できること（全体マップ）
| 分類 | 画面 |
|---|---|
| 毎日 | ダッシュボード／出席簿／心の天気／提出物／時間割 |
| 記録 | 観察記録／保健室／申し送りメモ（ポートフォリオ）／日誌 |
| 文書 | 授業案／通知表コメント／指導要録／IEP／学級通信／保護者連絡／週案 |
| 学級運営 | 児童生徒管理／係・掃除・席替え／面談スケジュール／参観・懇談／ランダム指名 |
| 分析 | 学級の成長ストーリー／授業デザイン4.0／データ分析 |
| 管理 | 設定（PIN・バックアップ・引き継ぎ・文字サイズ・ダークモード） |

### 1.2 動作環境
- 推奨ブラウザ：Google Chrome / Microsoft Edge（最新版）
- 起動方法：`index.html` をダブルクリック
- 初回のみインターネット接続が必要（表示用CDN・フォント）。以降はキャッシュで動作し、オフラインでも主要なUIは保持されます。
- カメラ・音声入力を使う場合、ブラウザの許可が必要です。許可されない環境でもテキスト／ファイル入力で代替できます。

### 1.3 画面の構成
- **ヘッダー**：☰（メニュー）／🌙（ダークモード）／Aa（文字サイズ）／🔍生徒検索／表示クラスフィルタ
- **サイドバー**：各機能へのナビ（未処理バッジが自動表示）
- **主画面**：選択した機能
- **下部ナビ（スマホ表示）**：ホーム／教材／通信／成績／連絡

---

## 2. 使い始めの手順（おすすめ順）

1. 【⚙️ 設定】基本情報（学校種・学校名・担任名・担任学年/クラス・年度）を入力 →「設定を保存」
   - 学校名・担任名は、以降の**PDF印字に自動反映**されます。
2. 【🧑 児童生徒管理】で生徒を登録（氏名・学年・組・誕生日任意・特記事項任意）
3. 【⚙️ 設定】プライバシー・安全設定で**PIN（4〜12桁の数字）**を設定 →「安全設定を保存」
4. 「暗号化バックアップ（推奨）」でバックアップファイルを保存（**PINは復旧不可のため控えも保管**）
5. お好みで 🌙 ダークモード／Aa 文字サイズ大 を設定
6. サイドバー「10分で使ってみる」でデモ体験（任意）

---

## 3. 共通の操作方法

### 3.1 移動と表示
- サイドバーのボタンで画面切替。現在地は濃い色で表示。
- **表示クラスフィルタ**（ヘッダー）：学年・組で一覧を絞り込み。「解除」で全体表示。
  - 出席・成績・席替え・面談表・通知表などは**フィルタ適用中の生徒が対象**になります。
- **🔍 生徒検索**：名前の一部を入力すると候補が表示され、クリックでポートフォリオへ直行。

### 3.2 文字サイズ・ダークモード
- **Aa**：標準⇔大 を切替（保存され次回も維持）。
- **🌙/☀️**：ダークモード切替（保存され次回も維持）。印刷（PDF）は常にライト表示。

### 3.3 ロックとショートカット
- **Ctrl+Shift+L**：即ロック。
- **Esc**：開いている確認/編集ウィンドウを閉じる。
- ロック中は画面全体が隠れ、PIN入力でのみ解除。再読込時もPIN必須。

### 3.4 保存・通知・確認
- 多くの操作はボタン押下で即保存（トースト＝黒い吹き出しで通知）。
- 削除などの重要操作は「確認」ダイアログ（はい/キャンセル）が出ます。
- **下書き自動保存**：学級通信・連絡文面・観察テキストは入力中も自動退避。誤って閉じても復元されます。

### 3.5 PDF・CSV出力
- 「PDF」ボタンは印刷画面を開きます。印刷ダイアログで「PDFに保存」を選べばPDF化。
  - ポップアップがブロックされたらアドレスバーのアイコンから許可してください。
- 「CSV出力」はExcel対応（BOM付き・数式注入ガード済み）。

---

## 4. 画面ごとの詳しい説明

### 4.1 10分で使ってみる（デモ）
1. 「デモデータを読み込む」：架空の1年1組（青木 葵さん他12名）と観察記録・テスト・提出物・心の天気を入力。**実データは上書きされません**。
2. 「ポートフォリオへ」→ 青木 葵さんを選択：時系列の成長と「次の仕事の候補」を表示。
3. 「学級の成長を見る」：学年=1年・学級=1組 を選択すると4軸（学習文化/協働/主体性/安心）の4か月変化。
4. 「授業デザインを見る」：材料と次の学びの候補を表示。
5. 「デモデータを削除」：デモ分だけ削除して元に戻す。

### 4.2 ダッシュボード
- **日付チップ**：今日の日付＋令和表示。
- **時間帯別ボタン**（朝/授業中/放課後/退勤前）：その時間帯の業務へワンタッチ移動。
- **今日の確認事項**：チェックリスト（毎日自動リセット）。
- **個人情報保護ステータス**：ロック・自動ロック・最終バックアップの状態（7日超過で警告）。
- **未処理・期限**：期限超過/今日期限の提出物、7日以内のイベント・面談。
- **8枚の数値カード**：生徒数・今月の観察・教材数・未提出・今日の欠席・保健室・心の天気嵐・要注目児童。
- **🎲 ランダム指名**：名前が回って止まる授業用ピック。
- **🕐 今日の授業**：今日の時間割から自動表示（時限番号付き）。
- **🎂 今週の誕生日**：登録済み誕生日から7日以内を表示（当日は🎉）。
- **最近の観察記録／今日の提出物チェック**。

### 4.3 授業案ジェネレーター
1. 学校種・学年・教科・単元・時間を入力 →「授業案を作成」。
2. 目標・流れ・評価規準・板書計画を含む案がプレビューに表示。
3. 「保存」で保存済み教材へ（表示/削除可）。「PDF」で印刷。

### 4.4 観察記録＋評価文案
1. 生徒・種類・**成長軸タグ（任意）**・日付を選択。
2. 入力タブ：
   - テキスト：そのまま入力（下書き自動保存）。
   - 音声：「🎤 開始」で喋ると文字化（対応ブラウザのみ・再タップで停止）。
   - カメラ：「カメラ起動」→「📷 撮影」でスナップショット添付→「停止」。
   - ファイル：クリックまたは**ドラッグ&ドロップ**で画像/動画/PDFの文件名を記録。
3. 「評価文案を作成」：記録をもとに評価文下書きを生成（必ず推敲を）。
4. 「記録を保存」。右側一覧は並び替え（新しい順/古い順/生徒別）・削除可。

### 4.5 通知表コメント（学期別保存）
1. 学期を選択（必要なら「学年評価」）→「評価文案を一括作成」。
2. 生徒ごとに編集可能な欄が並ぶので確認・修正。
3. 「💾 学期コメントを保存」で**学期単位の版として保存**。
4. 保存済み欄から「表示」（読込）/「PDF」/「削除」。

### 4.6 指導要録自動作成
1. 生徒と年度を選び「指導要録を自動生成」（出席率・係・観察記録数を自動参照）。
2. 4項目を編集（右プレビューに即時反映）→「保存」。
3. 保存済みは「表示/削除」、「PDF出力」可。年度は設定の年度が初期値。

### 4.7 成績管理
1. 「テストを登録」（名前・教科・満点・実施日）。
2. テストを選ぶと生徒別の点数欄（0〜満点に自動補正・入力即保存）。
3. 度数分布・個人別バー・統計（平均/最高/最低/標準偏差）が自動更新。
4. 「CSV出力」で点数一覧をダウンロード。

### 4.8 IEPテンプレート
各欄を入力→「IEPを保存」。保存済みは表示/PDF/削除。

### 4.9 時間割＆準備
- 時間割のマス（＋）をクリックして授業を追加・編集・削除。「授業を追加」ボタンも同様。
- **授業準備チェックリスト**：毎日自動リセット。
- **明日の授業準備リマインダー**：明日の時間割から自動表示。
- **準備メモ**：自由記入（保存）。引き継ぎパックにも掲載されます。

### 4.10 保護者連絡文例
1. シーン・生徒・トーン・追加情報を選び「文面を生成」。
2. 「コピー」/「保存」（履歴化）/「クリア」。履歴は表示/削除可。下書き自動保存対応。

### 4.11 提出物・連絡帳
1. 種類・内容・期限・対象を選び「登録」。
2. 提出物を選ぶと生徒別の「未提出/提出済」切替リスト。一括ボタンあり。
3. 期限超過はダッシュボードとサイドバーにバッジ表示。

### 4.12 学級通信
- テンプレート適用／キーワードの文章化で下書き作成→右側で編集→コピー/保存/PDF。保存済みは表示/削除。

### 4.13 児童生徒管理
- 登録/編集/削除。**誕生日（任意）**はダッシュボード「今週の誕生日」に反映（名簿に🎂チップ）。
- 「🖨 名簿PDF」で名簿を印刷。

### 4.14 出席簿
1. 日付を選び、生徒ごとに 出席/欠席/遅刻/早退 をタップ（即保存）。
2. 「全員出席」ワンタッチボタン。
3. 月次集計表と「CSV出力」「PDF」。

### 4.15 係・掃除当番・席替え
- 「自動生成」：生徒をシャッフルして係・掃除当番を割当。
- 「🔁 ローテーション」：メンバーを一人分繰り下げ。
- カスタム係の追加/削除（削除は正しい対象のみ削除されるよう修正済み）。
- **席替え**：列数を選び「席順を生成」。「手動調整」にチェックして2マスをクリックすると入替。「PDF」で座席表印刷。

### 4.16 クラス日誌・週案
- 「🤖 下書き自動作成」：今日の出席・心の天気・期限から日誌下書きを自動構成→修正→「保存」。
- 「週案生成」：時間割ベースの週案を作成・PDF。

### 4.17 授業参観・懇談会
- イベントを保存→選択して保護者の出席/欠席を登録→集計とPDF。一覧に「あとN日」表示。

### 4.18 面談スケジュール表 🗓
1. 日付・開始時刻・1枠時間を選び「面談表を生成」：**フィルタ適用中の生徒に時刻枠を自動割当**。
2. セッションを選択し、枠ごとに 生徒変更／状態（予定・完了・取消）／メモ を編集。
3. 「PDF出力」で面談表印刷。「選択セッションを削除」可。
4. 7日以内の面談はダッシュボード「未処理・期限」に表示。

### 4.19 保健室・心の天気
- 保健室利用を記録（一覧/傾向/PDF）。
- 心の天気：生徒を選び ☀️☁️🌧️⛈️ をタップ（即保存）。7日間傾向を表示。

### 4.20 ポートフォリオ
- 生徒を選択すると、観察・メモ・保健室・テスト・心の天気が時系列で表示。
- 「次の仕事の候補」を自動提案（参考情報）。
- **📝 申し送り・相談メモ**を追加可能（引き継ぎパックにも掲載）。

### 4.21 学級の成長ストーリー
- 学年/学級を選ぶと、4軸の4か月分の記録数と増減の解説を表示。観察記録の「成長軸タグ」を使うと精度向上。

### 4.22 授業デザイン4.0
- 観察記録・低得点テスト・提出率・心の天気から「気づくための材料」と「次の学びの候補」を整理。
- **最終判断は必ず先生が行ってください**（画面内にも注意書き）。

### 4.23 データ分析
- 「再分析」で、要注目生徒（スコア順）・傾向変化・出席×成績・提出物×観察の一覧を更新。

### 4.24 設定
- **基本情報**：学校名・担任名などはPDF印字に反映。年度は指導要録の初期値。
- **プライバシー・安全設定**：
  - PIN設定（4〜12桁数字・ソルト付きハッシュ保存）。空欄なら変更なし。
  - 自動ロック（しない/5/10/15/30分）。
  - 「安全診断を実行」：PIN・自動ロック・バックアップ・暗号化対応・保存領域・データ量を一覧表示。
  - 「🩺 全画面セルフテスト」：全画面のレンダリングを一括検査し✅/⚠️で報告。
  - 「画面をロック」。
- **データ管理・引き継ぎ**：
  - 使用量メーター（目安5MB中）。
  - 「暗号化バックアップ（推奨）」：PINでAES-GCM暗号化して保存。復元は「暗号化バックアップを復元」から（PIN必要）。
  - 「全データバックアップ（JSON）」/「復元（JSON）」：無暗号。取扱注意。復元ファイルは自動サニタイズ。
  - 「📦 引き継ぎパックPDF」：児童別の出席率・平均点・記録数・最新コメント・申し送りメモ＋学級メモを印刷。
  - 「📦 引き継ぎパックJSON」：後任の端末で「復元」から読み込める完全データ。
  - 「全データ削除」：確認後に完全削除（取り消せません）。

---

## 5. セキュリティと個人情報の取り扱い（重要）

1. データはすべて**この端末のブラウザ内**に保存されます。共用PCでは特にご注意ください。
2. **PINは忘れると復旧できません**。暗号化バックアップとPIN控えを安全な場所に保管してください。
3. ロック設定中は、再読込・別タブ起動時もPINが必要です。自動ロックは操作がないと発動します。
4. 引き継ぎパック・バックアップは個人情報そのものです。学校の規程に従い、安全な媒体で受け渡してください。
5. 学期末・異動時には「引き継ぎパック」出力後、「全データ削除」を検討してください。

---

## 6. 毎日の使い方の例

| 時間帯 | 操作 |
|---|---|
| 朝 | 出席簿（全員出席→欠席者だけ変更）→ 心の天気 → 今日の授業・誕生日確認 |
| 授業中 | 気づきを観察記録（成長軸タグ）→ ランダム指名 |
| 休み時間 | 提出物チェック → 保健室記録 |
| 放課後 | 成績入力 → 日誌（下書き自動作成）→ 保護者連絡 |
| 退勤前 | 学級通信 → 暗号化バックアップ → 要注目児童確認 → Ctrl+Shift+L でロック |

---

## 7. ショートカット一覧

| 操作 | 方法 |
|---|---|
| 画面ロック | Ctrl+Shift+L |
| ウィンドウを閉じる | Esc |
| 文字サイズ | ヘッダー Aa |
| ダークモード | ヘッダー 🌙/☀️ |
| 生徒検索 | ヘッダー 🔍（Enter不要・入力即検索） |

---

## 8. よくある質問・トラブル対応

| 症状 | 対処 |
|---|---|
| ボタンが反応しない | 画面を再読込。設定→🩺セルフテストで診断。 |
| カメラ/音声が使えない | ブラウザの許可を確認。`file://` で駄目なら Live Server 等で `http://localhost` 配信。テキスト/ファイル入力で代替可。 |
| PDFが出ない | ポップアップブロックを許可。 |
| PINを忘れた | 復旧手段はありません。平文バックアップがある場合は「全データ削除」後に復元（PIN設定はリセット）。 |
| 容量警告が出た | 設定の使用量メーターを確認。バックアップ後に古い記録/添付を整理。 |
| 別PCに移したい | 暗号化バックアップ→新PCで復元。または引き継ぎJSON。 |
| オフラインで使いたい | 初回読込後はキャッシュ動作。完全オフライン運用は将来課題（READMEロードマップ参照）。 |
| データが消えた？ | ブラウザの「サイトデータ削除」やプライベートモードで消失することがあります。必ず定期バックアップを。 |

---

## 9. バージョンとデータ移行

- 保存キー `kyomu_partner_v13` は v13〜v18 で共通。旧版のデータは自動で引き継がれます。
- v18の新機能（面談表・学期別コメント・引き継ぎ・ダークモード等）は、旧データから開いても自動的に利用可能になります。

---

*このマニュアルは v18 時点のものです。先生の毎日が、少しでも軽くなりますように。*